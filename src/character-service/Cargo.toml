[package]
name = "character-service"
version = "0.1.0"
authors = ["Ben Ukhanov"]
edition = "2018"

[dependencies]
diesel = { version = "1.4.4", features = ["postgres"] }
tonic = "0.2"
prost = "0.6.1"
tokio = { version = "0.2.20", features = ["macros"] }
dotenv = "0.15"

[build-dependencies]
tonic-build = "0.2"