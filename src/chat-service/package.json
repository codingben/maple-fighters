{
  "name": "chat-service",
  "version": "1.0.0",
  "description": "A chat service for communication between players.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "build": "esbuild src/server.js --outfile=bin/chat-service.js --bundle --platform=node --target=node16.8"
  },
  "author": "Ben Oukhanov <hello@codingben.io>",
  "dependencies": {
    "dotenv": "^10.0.0",
    "esbuild": "^0.14.1",
    "ws": "^8.2.2"
  }
}
