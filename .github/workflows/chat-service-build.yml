name: Chat Service Build

on:
  push:
    branches: [develop]
    paths:
      - src/chat-service/src/**

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup Node
        uses: actions/setup-node@v2
        with:
          node-version: '16'
          cache: 'npm'
          cache-dependency-path: src/chat-service/package-lock.json
      - name: Chat App Build
        run: npm install
        run: npm test
